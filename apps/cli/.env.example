# Unbound CLI Configuration
# These values are typically set during `unbound link`

# API Configuration
UNBOUND_API_URL=https://app.unbound.computer
UNBOUND_RELAY_URL=wss://relay.unbound.computer

# For local development
# UNBOUND_API_URL=http://localhost:3000
# UNBOUND_RELAY_URL=ws://localhost:8080

# Log level (debug, info, warn, error)
UNBOUND_LOG_LEVEL=info
