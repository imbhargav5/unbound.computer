[package]
name = "yagami"
version = "0.1.0"
edition = "2021"
description = "Directory listing utilities for daemon file tree loading"

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
tempfile = "3"
