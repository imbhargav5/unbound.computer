[package]
name = "observability"
version = "0.0.19"
edition = "2021"
authors = ["Unbound <team@unbound.computer>"]
license = "MIT"
description = "Centralized observability layer for the Unbound monorepo"
repository = "https://github.com/unbound-computer/unbound"

[dependencies]
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "json"] }
tracing-opentelemetry = "0.29"
opentelemetry = "0.28"
opentelemetry_sdk = { version = "0.28", features = ["rt-tokio", "experimental_trace_batch_span_processor_with_async_runtime"] }
opentelemetry-otlp = { version = "0.28", features = ["http-proto", "reqwest-client", "reqwest-rustls"] }

# HTTP client for OTLP exporter
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }

# Platform utilities
dirs = "6.0"
parking_lot = "0.12"

[dev-dependencies]
tempfile = "3"
